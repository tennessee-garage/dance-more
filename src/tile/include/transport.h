#pragma once
// Implementation lives in src/common/tile_bus_protocol/ so it is shared with
// src/row/ (the row controller is the other side of this same wire protocol).
#include "../../common/tile_bus_protocol/transport.h"
